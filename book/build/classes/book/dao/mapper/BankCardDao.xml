<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="book.dao.BankCardDao">
   <select id="queryCard" resultType="BankCard">
   select *from bank_card where kind=#{arg0} and card_number=#{arg1}
   
   </select>
	
</mapper>